= form_for @tweet do |f|
  - if @tweet.errors.any?
    #error_explanation
      %h2= "#{pluralize(@tweet.errors.count, "error")} prohibited this tweet from being saved:"
      %ul
        - @tweet.errors.full_messages.each do |msg|
          %li= msg
  %ul
  .form_group
    = f.text_field :content, placeholder: "今どんな気持ち？", class: "form-control"
    %br
    = f.file_field :image

  .actions
    %br
    %button.btn.btn-default{type: "submit", value: "送信"} つぶやく